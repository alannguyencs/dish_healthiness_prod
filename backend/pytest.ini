[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -m "not benchmark"
markers =
    benchmark: slow retrieval-quality benchmark (Stage 9); run nightly or manually via `pytest -m benchmark`.
filterwarnings =
    ignore::DeprecationWarning
